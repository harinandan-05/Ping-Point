engine-strict=true
legacy-peer-deps=false

# prevent package-lock.json creation
package-lock=false

# workspace support for pnpm/npm
workspaces-update=false

# Force consistent registry
registry=https://registry.npmjs.org/

# Make installs reproducible
save-exact=true
